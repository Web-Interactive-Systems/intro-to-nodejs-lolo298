/**
 Todo: complete this lab by importing the needed 
 objects and functions from
 ./utils/constants.js and ./utils/helpers.js
 */
const tmp = require("./utils/constants.js");
const hello = require("./utils/helpers.js");
console.log(tmp);
hello(foo);
hello(bar);
hello(baz);
